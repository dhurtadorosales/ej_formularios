{% extends 'layout.html.twig' %}

{% block content %}
    <h1 class="cla">Modificar grupo {{ grupo.descripcion }}</h1>

    {#{{ form(formulario) }}#}
    {{ form_start(formulario) }}
    {#{{ form_widget(formulario) }}#}
    {{ form_row(formulario.aula) }}
    <h6>Aquí podemos poner lo que queramos si usamos row en vez de widget</h6>
    {{ form_row(formulario.descripcion) }}
    <div clas="form-grupo">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            <button type="submit" name="enviar" class="btn btn-success btn"><i class="fa fa-check"></i> Guardar los camios</button>
            <button type="submit" name="eliminar" class="btn btn-danger btn"><i class="fa fa-trash"></i> Eliminar</button>
        </div>
    </div>
    {{ form_end(formulario) }}

{% endblock %}
